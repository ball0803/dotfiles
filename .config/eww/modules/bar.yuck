(defwidget bar []
  (box
    :class "bar-widget"
    :orientation "h"
    :space-evenly true

    (box :orientation "h" :space-evenly false :halign "start" :spacing 2
      (logo)
      (music)
    )

    (box :orientation "h" :space-evenly false :halign "center"
      (workspaces)
    )

    (box :orientation "h" :space-evenly false :halign "end" :spacing 2
      (big_clock)
      (indicator)
      (power_icon)
    )
  )
    
)
